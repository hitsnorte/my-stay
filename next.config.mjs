/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
      locales: ['english', 'portuguesePT'],  // Idiomas suportados
      defaultLocale: 'english',     // Idioma padrão
    },
  };
  
  export default nextConfig;
  